---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: ruby
    tag: '2.3.0'

params:
  DB_USER: vagrant
  DB_USER: vagrant
  DB_HOST: 127.0.0.1

inputs:
- name: spotlight-git

run:
  path: spotlight-git/run-test-in-concourse.sh